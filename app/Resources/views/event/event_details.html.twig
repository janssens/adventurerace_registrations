<div class="col-sm-6 col-md-4">
    <div class="thumbnail">
        <div class="caption">
            <h3>{{ event.title }}&nbsp;{% if is_granted('ROLE_ADMIN') %}{% if (event.owner == app.user) or is_granted("ROLE_SUPER_ADMIN") %}<a href="{{ path('event_edit', { 'id': event.id }) }}"><i class="fa fa-pencil fa-fw"></i></a>{% endif %}{% endif %}</h3>
            <div class="description">{{ event.description|raw }}</div>
                <p>
                    <a href="{{ path('event_show', { 'slug': event.slug }) }}" class="btn btn-default"><i class="fa fa-search fa-fw"></i>Détails</a>
                </p>
        </div>
    </div>
</div>
